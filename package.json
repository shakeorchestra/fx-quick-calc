{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build && cp dist/index.html dist/404.html && sh -c 'echo > dist/.nojekyll'",
    "deploy": "gh-pages -d dist -b gh-pages -m \"deploy\""
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
